# Musical Time Machine

Local development:

1. npm install
2. npm run dev

This project includes the SoundOfYou interactive component that generates personalized music recommendations and historical context based on a user's name and birth date. The component may call external APIs (Anthropic in the example) — provide appropriate API keys and CORS setup when integrating real APIs.
